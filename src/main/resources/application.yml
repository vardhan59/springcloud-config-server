---
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krishnamurthypradeep/springcloud-config
          search-paths:
           - 'station*'
  